services:
  shiny:
    image: gcampof/methylation4all-shiny:1.0.1
    container_name: m4a-shiny
    ports:
      - "3838:3838"
    volumes:
      - ./shiny/logs:/var/log/shiny-server
      - ./shiny/app/data:/srv/shiny-server/data
    # Cap the container's memory
    mem_limit: ${M4A_MEM_LIMIT:-24g}
    environment:
      - R_CONFIG_ACTIVE=default
    restart: unless-stopped
 
